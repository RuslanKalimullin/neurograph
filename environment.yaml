name: neurograph

channels:
  - pytorch
  - conda-forge
  - defaults
  - pyg

dependencies:
  - pip=22.3.1
  - python=3.10
  - cpuonly
  - pytorch
  - pyg
  - pip:
    - ipython
    - notebook
    - ipykernel
    - pandas
    - networkx==3.0
    - matplotlib
    - pytest
